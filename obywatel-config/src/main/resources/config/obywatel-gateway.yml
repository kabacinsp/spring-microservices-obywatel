server:
  port: 8080

spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: obywatel-auth
          uri: http://localhost:8081
          predicates:
            - Path=/auth/**